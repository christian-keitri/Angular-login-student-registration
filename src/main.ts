import { bootstrapApplication } from '@angular/platform-browser';
import { AppComponent } from './app/app.component'; // Import standalone AppComponent

bootstrapApplication(AppComponent);  // Bootstraps the standalone component
